server:
  port: 8888

spring:
  application:
    name: rent-app
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/rentapp
    username: rentapp
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL}
    password: ${MAIL_PASSWORD}
  data:
    redis:
      host: localhost


util:
  jwt:
    secret: cGY/4nWqKJg2u8Jmr+wgbfT5eZL5P70ErlC1DCpImiA=
    expiration: 10000

minio:
  url: http://127.0.0.1:9000
  username: minio
  password: password
  bucket: rentapp

logging:
  level:
    org:
      springframework:
        web: DEBUG
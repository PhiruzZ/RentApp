version: "3.5"

services:

  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: rentapp
      POSTGRES_DB: rentapp

  redis:
    image: redis:latest
    ports:
      - 6379:6379